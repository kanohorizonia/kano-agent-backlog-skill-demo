# Project-Level Backlog Configuration
# Manages kano-agent-backlog-skill and kano-commit-convention-skill products

# Global defaults applied to all products
[defaults]
skill_developer = true
auto_refresh = true

# Optional: default profile overlay (CLI --profile overrides this)
# Examples:
#   profile = "embedding/local-noop"
#   profile = "embedding/gemini-embedding-001"
#
# Built-in profiles (repo):
#   - skills/kano-agent-backlog-skill/profiles/embedding/local-noop.toml
#   - skills/kano-agent-backlog-skill/profiles/embedding/gemini-embedding-001.toml
#
# If you prefer project-local profiles, place them under:
#   .kano/backlog_config/embedding/<name>.toml
# and use shorthand refs like: "embedding/<name>"
#
# profile = "embedding/local-noop"

# Product definitions for this project
[products.kano-agent-backlog-skill]
name = "kano-agent-backlog-skill-demo"
prefix = "KABSD"
backlog_root = "_kano/backlog/products/kano-agent-backlog-skill"
analysis_llm_enabled = true
vector_enabled = true

[products.kano-commit-convention-skill]
name = "kano-commit-convention-skill"
prefix = "KCC"
backlog_root = "_kano/backlog/products/kano-commit-convention-skill"
vector_enabled = true

# Shared settings for all products
[shared.log]
debug = false

# Cache configuration (unified for all chunks and vectors)
[shared.cache]
# Unified cache root directory for all products
# All chunks databases and vector indexes will be stored here
# Can be overridden with --cache-root CLI parameter
root = ".kano/cache/backlog"

[shared.vector]
enabled = true
backend = "sqlite"
collection = "backlog"
metric = "cosine"

# Added by kano-backlog admin init (agent=opencode)
[products.native-admin-smoke]
name = "native-admin-smoke"
prefix = "NAS"
backlog_root = "native_init_backlog/products/native-admin-smoke"
